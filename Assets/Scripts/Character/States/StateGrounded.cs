namespace Character.States
{
    public class StateGrounded : StateMoving
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }

        public StateGrounded(Character character, StateMachine stateMachine) : base(character, stateMachine)
        {
        }
    }
}
